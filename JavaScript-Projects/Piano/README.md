# JS Piano 🎹

A lightweight web piano that plays audio samples when you press keyboard keys. It highlights the pressed key, shows the current note, and displays hints on hover.

---

## 🚀 Features

- Play piano notes using your computer keyboard
- Visual key highlight and `Now Playing` display
- Hover hints showing the corresponding keyboard key
- Background video for visual flair

---

## 📁 Project structure

- `index.html` — main markup
- `style.css` — styles and visual effects
- `script.js` — keyboard event handling and audio playback


---

## ▶️ Usage

1. Open `index.html` in your browser (double-click or open via Live Server / simple HTTP server).
2. Press the keys shown on the keys (A, W, S, E, etc.) to play notes.

Tip: If audio is blocked by the browser, click the page once or start with a key press (user gesture) to allow playback.


---

## ⌨️ Keyboard mapping

| Key | Note |
|-----|------|
| A   | C    |
| W   | C#   |
| S   | D    |
| E   | D#   |
| D   | E    |
| F   | F    |
| T   | F#   |
| G   | G    |
| Y   | G#   |
| H   | A    |
| U   | A#   |
| J   | B    |
| K   | C    |
| O   | C#   |
| L   | D    |
| P   | D#   |
| ;   | E    |

---

## ⚙️ Customization

- Replace the `<audio>` `src` URLs in `index.html` to use local samples.
- Change styles in `style.css` to modify the look and animation.
- Update `script.js` if you want different key bindings or extra behavior.

---


> **Note:** This is an educational project — attribution to original audio/video sources is recommended if reused.